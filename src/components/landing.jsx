import React from 'react';

const Landing = () => {
  return (
    <div>
      <div>
        <h1>Landing</h1>
        <button onClick={() => alert('show counter')}>Show counter</button>
      </div>
      <div>
        <h1>React useEffect</h1>
      </div>
    </div>
  );
};

export default Landing;
